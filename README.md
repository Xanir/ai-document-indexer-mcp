This project reads project documents stored in a GitHub repo.
The top level folders are treated as seperate containers (hard isolation)
The contents are parsed into a vector database and made available via an MCP server
